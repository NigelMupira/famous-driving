function beingDeveloped(){
	alert("UNDER CONSTRUCTION!!!")

/*
// function to alert the user that they are being redirected to our other platforms after clicking the links
function confirmLink(link) {
  const confirmed = confirm("Are you sure you want to visit this link?");
  if (confirmed) {
    window.location.href = link.href;
  }
}

const myLink = document.getElementById("myLink");
myLink.addEventListener("click", function(event) {
  event.preventDefault(); // Prevent default link behavior
  confirmLink(this); // Call the confirmLink function with the clicked link
});
*/